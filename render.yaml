services:
  - type: web
    name: sinhangman-gas
    runtime: docker
    plan: free
    region: singapore
    branch: master
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /
    autoDeploy: true
envVars:
  - key: PORT
    value: "8080"